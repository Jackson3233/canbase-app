import React from "react";
import { ChatScreen } from "@/screens/chat";
import { Wrapper } from "@/components";

const Chat: React.FC = () => {
  return (
    <Wrapper>
      <ChatScreen />
    </Wrapper>
  );
};

export default Chat;

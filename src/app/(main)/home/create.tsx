import React from "react";
import { CreateClubScreen } from "@/screens/dashboard";
import { Wrapper } from "@/components";

const CreateClub: React.FC = () => {
  return (
    <Wrapper>
      <CreateClubScreen />
    </Wrapper>
  );
};

export default CreateClub;

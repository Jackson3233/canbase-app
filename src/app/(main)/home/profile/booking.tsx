import React from "react";
import { BookingScreen } from "@/screens/profile";
import { Wrapper } from "@/components";

const Booking: React.FC = () => {
  return (
    <Wrapper>
      <BookingScreen />
    </Wrapper>
  );
};

export default Booking;

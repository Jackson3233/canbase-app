import React from "react";
import { ReservationScreen } from "@/screens/shop";
import { Wrapper } from "@/components";

const Chat: React.FC = () => {
  return (
    <Wrapper>
      <ReservationScreen />
    </Wrapper>
  );
};

export default Chat;
